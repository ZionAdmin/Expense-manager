<div class="col-xs-12">
  <div class="card">
    <div class="header">Edit Expense Register</div>
    <div class="body">
      <div class="row">
        <!--Daily Invoices starts-->
        <%= form_for @daily_invoice, url: daily_invoice_path(@daily_invoice), method: "patch" do |f| %>
            <% render partial: 'form', locals: {f: f} %>
        <% end %>
        <!--Daily Invoices ends-->
      </div>
    </div>
  </div>
</div>
</div>
